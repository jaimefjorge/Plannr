@model IEnumerable<Plannr.Models.Enseignement>


@{
    ViewBag.Title = "Index";
}

<!--<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Libelle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Libelle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>

@{
    ViewBag.Title = "IndexSalle";
}-->

<div id="choixContainer">
<div id ="content-header">
<h1>Gestion des enseignements</h1>
</div>
<div id="breadcrumb">
				<a href="#" class="tip-bottom" data-original-title="Go to Home"><i class="icon-home"></i> Home</a>
				<a href="#" class="tip-bottom"> Gestion Enseignements</a>
                
			</div>



<div class="container-fluid">
    <div class="alert">
    <button class="close" data-dismiss="alert">×</button>
    
</div>
    <div class="row-fluid">
        <div class="span12">
        <div class="widget-box">
            <div class="widget-title">
								<span class="icon">
									<i class="icon-th"></i>
								</span>
                            <h5>Liste des enseignements</h5>
                        </div>
            <div class="widget-content nopadding">
  <table class="table table-bordered">
    <tr >
        <th>
            @Html.DisplayNameFor(model => model.Libelle)
         </th>
        <th>Cours</th>
        <th>Enseignant</th>
        <th>Groupe</th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr >
        <td>
            @Html.DisplayFor(modelItem => item.Libelle)
            </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cours.Libelle)
            </td>
        <td>
            @Html.DisplayFor(modelItem => item.Enseignant.UserName)
            </td>
        <td>
            @Html.DisplayFor(modelItem => item.Groupe.Libelle)
            </td>
       
       
        <td>
            <center>
            <a href="@Url.Action("Delete", "Enseignement", new { id=item.Id })" class="btn btn-danger"><i class="icon-remove icon-white"></i></a>
            </center>
    </td>
    </tr>
}

</table>
        </div>
        

        

    </div>
    </div>
        </div>
</div>
<center>
<a href="@Url.Action("Create", "Enseignement")" class="btn btn-large btn-primary" id="add"><i class="icon-pencil icon-white"></i>Créer un nouvel enseignement</a>
</center>
    </div>

<div id="editContainer" style="display: none;">
</div>

<script type="text/javascript">

    var editLoaded = false;

    function showEdit() {
        $('#choixContainer').fadeOut("fast", function () {
            $('#editContainer').fadeIn()
            editLoaded = false;
        })

    }

    function showIndex() {
        $('#editContainer').fadeOut("fast", function () {
            $('#choixContainer').fadeIn()
            editLoaded = false;
        })

    }

    $('#cancelEdit').live('click', function (e) {

        showIndex()

        e.preventDefault()
        return false
    })

    $('#add').live('click', function (e) {
        if (!editLoaded) {
            $.get("@Url.Action("Create","Enseignement")", function (data) {

                $('#editContainer').html(data)
                showEdit()
                editLoaded = true
            })

        } else {
            showEdit()
        }
        e.preventDefault()
        e.stopPropagation()
        return false
    })

     </script>