@model Plannr.ViewModels.DemandeReservationEnseignement

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>DemandeReservation</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.DemandeReservation.DateDemande)
        </div>

        @Html.DropDownListFor(x => x.Enseignements, new SelectList(ViewBag.Enseignements, "Id", "Lib"));
        
        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
