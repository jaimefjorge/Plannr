@model Plannr.Models.DemandeReservation

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>DemandeReservation</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.DateDemande)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.DateDemande)
            @Html.ValidationMessageFor(model => model.DateDemande)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Checked)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Checked)
            @Html.ValidationMessageFor(model => model.Checked)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
